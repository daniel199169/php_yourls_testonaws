<?php        
        echo "<h1>Test shorten url</h1>";
        echo "<a href='http://3.228.164.200/M'>http://3.228.164.200/M</a>";
 
        // $username = 'lsohealthcare_alexey';
        // $password = 'Timeismoney!12345';

        // $format  = 'simple';                       // output format: 'json', 'xml' or 'simple'

        // // EDIT THIS: the URL of the API file
        // $url = "https://freelancer.com";
        // $api_url = 'http://cesar.lsohealthcare.com/yourls-api.php';

        // // Init the CURL session
        // $ch = curl_init();
        // curl_setopt( $ch, CURLOPT_URL, $api_url );
        // curl_setopt( $ch, CURLOPT_HEADER, 0 );            // No header in the result
        // curl_setopt( $ch, CURLOPT_RETURNTRANSFER, true ); // Return, do not echo result
        // curl_setopt( $ch, CURLOPT_POST, 1 );              // This is a POST request
        // curl_setopt( $ch, CURLOPT_POSTFIELDS, array(      // Data to POST
        //         'url'      => $url,
        //         'format'   => $format,
        //         'action'   => 'shorturl',
        //         'username' => $username,
        //         'password' => $password
        // ) );

        // // Fetch and return content
        // $data = curl_exec($ch);
        // curl_close($ch);

        // // Do something with the result. Here, we just return it.
        // echo "<pre>";
        // print_r($data);
        // echo "</pre>";
?>